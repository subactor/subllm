# Codex execution note

The user authorized autonomous implementation. The change remains bounded to
the POA runtime: it validates a candidate DSL and emits evidence, but never
writes a process definition, selects an executable, expands authority or
publishes the proposal.
