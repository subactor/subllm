# Changelog

- Added bounded environment overrides for the provider attempt deadline and
  slow-response threshold without introducing a private policy copy.
- Closed after protected merge, immutable deployment and successful Supervisor
  readback using the tuned sequential failover policy.
