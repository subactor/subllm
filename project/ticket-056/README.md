# ticket-056 — Draft configuration context

Bounded intent: include nonignored untracked source drafts in the private snapshot, call the canonical explicit configuration input API, and validate absent top-level JSON field additions against selected aggregate evidence.

Canonical documentation: [code2dsl context](../../docs/information/code2dsl-context.md).
