# Project notes

The first migration targets the Python agent fleet. Non-Python callers should
consume the CLI's public fields or a future generated projection rather than
copying the policy by hand.

